# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# OS generated files
.DS_Store
.DS_Store?
/.streamlit/user_settings.json
/.streamlit/user_settings.tmp
/.streamlit/last_golden_report.json
/.streamlit/last_golden_report.tmp
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Data files (optional, depending on if we want to commit the generated data)
# For this take-home, we probably want to commit the mock input data, 
# but ignore large outputs if any. We'll leave data/ un-ignored for now, 
# except maybe dynamic stream files.
data/chat_stream.jsonl
data/snapshot.json
data/market_signal.db

# Private take-home assignment prompt / specifications
docs/AI_Engineer_Take_Home_Market_Signal_Bot.pdf
*.pdf
